# Halogen-Dial-Up-Firefox-Extension

This is a simple browser extension for Firefox for Desktop. The extension play a dial-up sound before starting to load a website.

# ! Warning !
This extension is unsigned for Firefox. It means that by downloading the extension you can only use it by adding it temporary in about:debugging on Firefox
